client localhost {
    ipaddr          = 127.0.0.1
    secret          = testing123
    shortname       = localhost
    nas_type        = other
}

client docker_network {
    ipaddr          = 172.0.0.0/8
    secret          = testing123
    shortname       = docker
    nas_type        = other
}